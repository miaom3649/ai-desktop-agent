ai:
  backend: ollama          # ollama | gemini | claude | openai
  ollama_model: qwen2.5-vl:7b
  ollama_base_url: http://localhost:11434
  api_key: ""              # 云端 BYOK 时填入

safety:
  require_confirmation_level: 2  # >= 此等级的动作需要 GUI 确认

hotkeys:
  toggle_floating: "ctrl+shift+space"

screenshot:
  max_width: 1280
  jpeg_quality: 85
